function add (a, b) {
  return a + b
}
// The add function has parametres in brackets() of a & b. 
// It will return the variables in the brackets added together.
// add (3, 4) will return 7
// There is no need to put function add, just add().

module.exports = add
// Enables an export of the add function to other files