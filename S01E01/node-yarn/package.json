{
  "name": "node-yarn",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.3.3",
    "cowsay": "^1.4.0"
  }
}

// Dependancies are the packages which your app depends on
// Package.json keeps track of all the packages we depend on
// In this case, the dependancies are colors and cowsay which are packages with code in them

